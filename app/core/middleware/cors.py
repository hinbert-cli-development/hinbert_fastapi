"""CORS policy documentation and extension point.

The actual middleware is installed in ``app.main`` from validated settings.
Keeping policy data in settings makes allowed origins auditable per deployment.
"""
